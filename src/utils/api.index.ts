// src/utils/api.index.ts
// Re-export everything from api.ts for backward compatibility
// All imports from '@/utils/api.index' now resolve to the same modules as '@/utils/api'

export * from './api';
